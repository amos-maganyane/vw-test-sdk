# Scope @enviro365 packages to GitHub Packages. The auth token is NEVER
# committed here — it lives in the user-global ~/.npmrc (write:packages PAT)
# or the CI NODE_AUTH_TOKEN env var. See README "Publishing".
@enviro365:registry=https://npm.pkg.github.com
registry=https://registry.npmjs.org/
auto-install-peers=true
